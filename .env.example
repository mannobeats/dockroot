# Optional production overrides for single-file Docker installs.
# Dockroot generates internal secrets automatically on first boot and stores them in `dockroot_data`.

# Set this when deploying behind a reverse proxy or custom domain.
# APP_URL=https://dockroot.example.com

# Optional: explicit auth origin allowlist (comma-separated).
# Useful when users access Dockroot from multiple origins (domain + LAN IP).
# BETTER_AUTH_TRUSTED_ORIGINS=https://dockroot.example.com,http://10.0.10.70:3080

# Optional: override state signing secret for GitHub App onboarding flow.
# GitHub App credentials (app id/private key/webhook secret) are managed in-product and
# stored encrypted in the database; no GitHub credential env vars are required.
GITHUB_APP_STATE_SECRET=
